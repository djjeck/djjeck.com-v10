// Simple test to verify Jest configuration
describe('Basic tests', () => {
  it('should pass a simple test', () => {
    expect(true).toBe(true);
  });

  it('should do simple math correctly', () => {
    expect(1 + 1).toBe(2);
  });
});