{
  "name": "Start application",
  "description": "Start the application using direct execution to bypass TypeScript checks",
  "tasks": [
    {
      "name": "Start Server",
      "command": "./direct-start.sh",
      "waitForPort": 5000
    }
  ]
}